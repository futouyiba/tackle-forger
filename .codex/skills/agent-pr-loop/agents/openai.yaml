interface:
  display_name: "Agent PR Loop"
  short_description: "用多 Agent 完成单个 PR 的评论、复审、CI 与自动合并闭环"
  default_prompt: "Use $agent-pr-loop to resolve, review, verify, and complete the active pull request."
policy:
  allow_implicit_invocation: true
