interface:
  display_name: "Agent PR Loop"
  short_description: "用多 Agent 完成单个 PR 的评论、复审、CI 与集成证据闭环"
  default_prompt: "Use $agent-pr-loop to resolve, review, and verify exact-head integration evidence for the active pull request, and use its safe readback procedure if a merge is performed."
policy:
  allow_implicit_invocation: true
