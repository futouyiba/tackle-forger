{
  "name": "@tackle-forger/ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --config ../../vitest.workspace.config.ts --passWithNoTests"
  },
  "dependencies": { "react": "19.2.4" },
  "devDependencies": { "@types/react": "^19.1.8", "typescript": "^5.8.3", "vitest": "^3.2.4" }
}
